#include<iostream>
using namespace std;
const int MAX_SIZE = 100;

int main() {
    int N;
    cin >> N;
    
    int arr[MAX_SIZE];
    for(int i = 0; i < N; i++) {
        
    }
    for(int i = 0; i < N; i++) {
        if(i % 2 == 0) {
            cout << i << " " << endl;
        }
    }
    
    return 0;
}
